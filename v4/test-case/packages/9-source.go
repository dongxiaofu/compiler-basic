package main

import "fmt"

func swap(x *int, y *int) {
	*res = a
	res = a
	 *x = *y
   *y = temp + 54 - d + 89 * f - 100
y = temp + 54 - d + 89 * f - 100;
y = temp + 54 * d + 89 * f - 100
y = *temp + 54 * d + 89 * f - 900
return 5

	var a int = 100
   var b int= 200

   fmt.Printf("交换前 a 的值 : %d\n", a )
   fmt.Printf("交换前 b 的值 : %d\n", b )

	var temp int;var temp int
}
