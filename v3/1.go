const (
	age  uint = 580 ^ 90 / 2 << float32(value,) + sum(uint, a,b,c...) / 100
	age  uint = 580 ^ 90 / 2 << float32(value,) + sum(uint, a,b,c...);
	age  uint = 580 ^ 90 / 2 << float32(value,) + sum(uint, a,b,c...);
	age  uint = 580 ^ 90 / 2 << float32(value,) + sum(uint, a,b,c...);
	age  uint = 580 ^ 90 / 2 << float32(value,) + sum(uint, a,b,c...);
	age  uint = 580 ^ 90 / 2 << float32(value,) + sum(uint, a,b,c...);
)
